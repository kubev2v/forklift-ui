import * as React from 'react';

interface IAddEditNetworkMappingProps {
  isEdit: boolean;
}

const AddEditNetworkMapping: React.FunctionComponent<IAddEditNetworkMappingProps> = ({
  isEdit,
}: IAddEditNetworkMappingProps) => {
  return <div>TODO: AddEditNetworkMapping</div>;
};

export default AddEditNetworkMapping;
