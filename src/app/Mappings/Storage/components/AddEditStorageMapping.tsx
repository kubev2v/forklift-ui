import * as React from 'react';

interface IAddEditStorageMappingProps {
  isEdit: boolean;
}

const AddEditStorageMapping: React.FunctionComponent<IAddEditStorageMappingProps> = ({
  isEdit,
}: IAddEditStorageMappingProps) => {
  return <div>TODO: AddEditStorageMapping</div>;
};

export default AddEditStorageMapping;
